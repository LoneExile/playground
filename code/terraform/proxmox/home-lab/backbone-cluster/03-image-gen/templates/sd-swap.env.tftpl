# Rendered by Terraform — consumed by sd-swap.service (EnvironmentFile).
SDSWAP_PORT=${port}
SDSWAP_BACKEND_PORT=${backend_port}
SDSWAP_REGISTRY=${sd_dir}/models.json
SDSWAP_MODELS_DIR=${models_dir}
SDSWAP_BIN=${sd_dir}/src/build/bin/sd-server
SDSWAP_LIB=${sd_dir}/src/build/bin
SDSWAP_EXTRA_FLAGS=${extra_flags}
SDSWAP_TTL=${ttl}
SDSWAP_LOAD_TIMEOUT=${load_timeout}
SDSWAP_GEN_TIMEOUT=${gen_timeout}
