# Homelab login theme. Parent on keycloak.v2 so every non-login page (error,
# info, OTP, account) still renders with the modern stock theme; login.ftl is
# fully overridden below with a self-contained custom page.
parent=keycloak.v2
